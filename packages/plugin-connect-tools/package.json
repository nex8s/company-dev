{
  "name": "@paperclipai/plugin-connect-tools",
  "version": "0.1.0",
  "description": "Connect-tools plugin (B-14) — pluggable adapter pattern for Notion / Slack / Figma / GitHub / Linear / Vercel connections plus a per-company connections store.",
  "license": "MIT",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./server/index": "./src/server/index.ts"
  },
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "typecheck": "tsc --noEmit",
    "test:run": "vitest run"
  },
  "dependencies": {
    "@paperclipai/db": "workspace:*",
    "drizzle-orm": "^0.38.4",
    "express": "^5.1.0",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^24.6.0",
    "@types/supertest": "^6.0.2",
    "supertest": "^7.0.0",
    "typescript": "^5.7.3",
    "vitest": "^3.0.5"
  }
}
