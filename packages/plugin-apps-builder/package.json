{
  "name": "@paperclipai/plugin-apps-builder",
  "version": "0.1.0",
  "description": "Apps builder plugin — App model, builder worker loop, preview/code/deploy/env endpoints",
  "license": "MIT",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./schema": "./src/schema.ts"
  },
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "typecheck": "tsc --noEmit",
    "test:run": "vitest run"
  },
  "dependencies": {
    "drizzle-orm": "^0.38.4"
  },
  "devDependencies": {
    "@types/node": "^24.6.0",
    "typescript": "^5.7.3",
    "vitest": "^3.0.5"
  }
}
